# BDF

This folder contains a BDF font parser and renderer. The purpose of this is to be able
to do simple text rendering with backends that don't support any built-in ways of rendering
text, such as wayland


